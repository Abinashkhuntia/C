//find the number is divisible by 97 or  not ?
#include <stdio.h>
#include <math.h>
int main()
{
  int a ;
  printf("enter a no");
  scanf("%d",&a);
  if(a%97==0)
    printf("divisible");
  else
    printf("not divisible");
  return 0 ;
}