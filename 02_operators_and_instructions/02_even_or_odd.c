// enter a no to find its even or odd ?
#include <stdio.h>
#include <math.h>
int main()
{
    int a ,b ;
    printf("enter  no  \n" );
    scanf("%d",&a);

    if (a%2==0)
        printf("its a even no");
    
    else
        printf("noo its no a true no");
    
    return 0 ;
}